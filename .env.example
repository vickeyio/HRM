# API Base URL for the backend REST API
VITE_API_BASE_URL=http://localhost:3000/api

# Set to 'true' to use the built-in mock service adapter (no backend required)
# Set to 'false' to use the real backend API
VITE_USE_MOCK=true
